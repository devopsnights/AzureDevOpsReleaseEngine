trigger:
  branches:
    include:
    - main
    - feature/v0.1

pool:
  vmImage: 'windows-latest'

variables:
  - template: ./variables/dev-vars.yml

extends: 
  template: ../../../../main.yml
  parameters:
    settings:
      build:
        enabled: true
    resources:
      - name: RG-example
        type: resourceGroup
        enabled: true
        azureServiceConnection: ServiceConnection