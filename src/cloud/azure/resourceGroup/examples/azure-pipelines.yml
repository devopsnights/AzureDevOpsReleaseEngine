trigger:
  branches:
    include:
    - main
    - feature/v0.1

pool:
  vmImage: 'windows-latest'


extends: 
  template: ../../../../main.yml
  parameters:
    settings:
      build:
        enabled: false
      deploy:
        enabled: true
      azureServiceConnection: $(azureServiceConnection)
      randomVariable: $(randomVariable)
      environments:
        - dev
        - uat
    resources:
      - name: RG-example
        type: resourceGroup
        enabled: true
        